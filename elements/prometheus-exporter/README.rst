====================
Prometherus exporter
====================

This element, used only in Destination Earth images is used to install the prometheus exporter into the image but from the binaries compiled at ECMWF and not from the upstream tarball. 
